lyldalek.top {
	# 根路径与无斜杠的 /kidstar 都重定向到 /kidstar/
	redir / /kidstar/ 308
	redir /kidstar /kidstar/ 308

	# 主入口: /kidstar/* 剥掉前缀转发给应用
	handle_path /kidstar/* {
		reverse_proxy app:3000
	}

	# 其余路径(站内导航 / 静态资源 / api/auth)透传
	reverse_proxy app:3000
}
